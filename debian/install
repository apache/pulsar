# Copy the entire pulsar distribution to /usr/lib/apache-pulsar
# This includes all JARs, scripts, and other files from the distribution
# Note: conf/ is a symlink to /etc/apache-pulsar created in debian/rules
debian/tmp/pulsar /usr/lib/apache-pulsar

# конфиги идут в /etc/apache-pulsar (moved in debian/rules, symlink created for compatibility)
debian/tmp/etc/apache-pulsar/conf /etc/apache-pulsar

# символические ссылки для основных команд
debian/tmp/usr/bin/* /usr/bin

# дополнительные systemd сервисы как примеры (не активируются автоматически)
debian/tmp/usr/share/apache-pulsar/examples/systemd/*.service /usr/share/apache-pulsar/examples/systemd